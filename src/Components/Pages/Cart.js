// Cart.js
import React from 'react';
// import { useCart } from './CartContext';

const Cart = () => {
  // const { state } = useCart();
  // const { cartItems } = state;

  return (
    <div>
      <h2>Shopping Cart</h2>
      {/* <ul>
        {cartItems.map((item) => (
          <li key={item.productId}>
            <p>{item.carName}</p>
          </li>
        ))}
      </ul> */}
    </div>
  );
};

export default Cart;
